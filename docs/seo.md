# OptiCV — SEO Keyword Analysis

_Generated: 2026-06-04_

## What the site already does well

The current title and meta description are solid. The copy naturally uses several high-value terms: _ATS_, _CV optimization_, _keyword gap analysis_, _cover letter_, _interview prep_. The `/what-is-ats` page is a smart move for informational SEO.

---

## Recommended Keywords by Category

### Primary (High Intent — people ready to act)

| Keyword | Why it fits |
|---|---|
| `AI CV optimizer` | Core product description |
| `ATS CV checker` | Your #1 feature |
| `CV optimization tool` | Direct product category |
| `optimize CV for job application` | Describes the exact user action |
| `ATS resume checker free` | Free tier + high commercial intent |
| `tailor CV to job description` | Step 2 of your workflow |

### Secondary (Problem-Aware — people who know their pain)

| Keyword | Why it fits |
|---|---|
| `why is my CV rejected` | Your Resume Autopsy feature solves this |
| `CV keyword gap analysis` | Already a named feature — use it in headers |
| `beat ATS filters` | Used in copy, should be in meta too |
| `ATS-friendly CV` | Common search query, used in your hero |
| `CV not getting interviews` | High-empathy entry point |
| `how to pass ATS screening` | Matches your `/what-is-ats` page |

### Feature-Level (Long-tail — lower volume, higher conversion)

| Keyword | Where to use it |
|---|---|
| `AI cover letter generator` | Features page, Tailored Cover Letters section |
| `interview preparation tool` | Features page, Interview Prep section |
| `CV bullet point rewriter` | Stronger Bullet Points feature |
| `professional summary generator` | Summary Rewrites feature |
| `LinkedIn profile optimizer` | Coming Soon section |
| `STAR method CV bullets` | Niche but precise — signals expertise |

### Informational / Blog (Top of funnel — build authority)

| Keyword | Content angle |
|---|---|
| `what is an ATS` | You already have this page — good |
| `how to write a CV for ATS` | Blog post |
| `ATS resume tips` | Blog post |
| `why recruiters reject CVs` | Blog post (your stats section already has this data) |
| `CV vs resume difference` | Blog post — captures UK + US audiences |
| `how long do recruiters spend reading a CV` | Blog post (you cite the 7-second stat — expand it) |

---

## Quick Wins — Specific Fixes

1. **FAQ page still has "Foxi" template content** — the title says _"Foxi | Get Your Questions Answered"_. Fix the `<title>` and `<description>` to match OptiCV. This is a ranking liability.

2. **Pricing page description is weak** — _"Check OptiCV for free and then upgrade to pro"_ misses keywords entirely. Suggested replacement: _"Start free with OptiCV's ATS CV checker. Upgrade to Pro for unlimited CV optimizations, cover letters, and interview prep."_

3. **Features page title** — the current title is great but long. Consider testing: _"OptiCV Features — ATS CV Checker, Keyword Gap Analysis & More"_ to front-load keywords.

4. **Blog** — you have a `/blog` in the nav. If it's empty or thin, either populate it with the informational posts above or hide it until launch. Thin/empty blog pages can hurt rather than help.

5. **UK vs US spelling** — your audience likely spans both. Your copy uses _CV_ (British English) throughout, which is correct for UK/EU job seekers. If you also want to capture US traffic, consider adding _resume_ in alt text, meta descriptions, or a dedicated page. Right now you're clearly UK-focused, which is a valid choice — just be intentional about it.

---

## Top 5 Keywords to Target Now

| Priority | Keyword | Rationale |
|---|---|---|
| 1 | `ATS CV checker` | High intent, matches your core feature |
| 2 | `AI CV optimizer` | Product category, growing search volume |
| 3 | `tailor CV to job description` | Describes exactly what you do |
| 4 | `CV keyword gap analysis` | Specific, lower competition, feature-named |
| 5 | `what is an ATS` | Informational, you already own this page |
